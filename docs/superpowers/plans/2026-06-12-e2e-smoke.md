# Smoke test E2E (Playwright) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Una suite E2E headless e riproducibile (Playwright) che verifica il boot della shell online + offline senza errori console e senza dipendenze da CDN, come rete di sicurezza per il refactor di `app.js`.

**Architecture:** Server statico node zero-dipendenze (`scripts/static-server.cjs`) avviato dal `webServer` di Playwright; i test in `e2e/*.spec.js` girano con `npm run e2e`, separati dalla suite unit `node --test` che resta veloce.

**Tech Stack:** `@playwright/test` (devDep), node `http`/`fs`, service worker.

Spec: `docs/superpowers/specs/2026-06-12-e2e-smoke-design.md`

---

### Task 1: Server statico zero-dipendenze

**Files:**
- Create: `scripts/static-server.cjs`

- [ ] **Step 1: Scrivi il server**

`scripts/static-server.cjs`:
```js
// scripts/static-server.cjs — server statico zero-dipendenze per E2E e sviluppo.
// Uso: node scripts/static-server.cjs [porta]   (default 8766)
const http = require("http");
const fs = require("fs");
const path = require("path");

const ROOT = path.join(__dirname, "..");
const PORT = Number(process.argv[2] || process.env.PORT || 8766);

const TYPES = {
  ".html": "text/html; charset=utf-8",
  ".js": "text/javascript; charset=utf-8",
  ".mjs": "text/javascript; charset=utf-8",
  ".cjs": "text/javascript; charset=utf-8",
  ".json": "application/json; charset=utf-8",
  ".css": "text/css; charset=utf-8",
  ".svg": "image/svg+xml",
  ".png": "image/png",
  ".ico": "image/x-icon",
};

const server = http.createServer((req, res) => {
  let pathname = decodeURIComponent(req.url.split("?")[0]);
  if (pathname === "/") pathname = "/index.html";
  const filePath = path.join(ROOT, path.normalize(pathname));
  if (!filePath.startsWith(ROOT)) { res.writeHead(403).end("forbidden"); return; }
  fs.readFile(filePath, (err, buf) => {
    if (err) { res.writeHead(404).end("not found"); return; }
    const type = TYPES[path.extname(filePath)] || "application/octet-stream";
    res.writeHead(200, { "Content-Type": type });
    res.end(buf);
  });
});

server.listen(PORT, () => console.log(`static-server su http://localhost:${PORT} (root: ${ROOT})`));
```

- [ ] **Step 2: Smoke manuale del MIME dei .js**

Run:
```bash
node scripts/static-server.cjs 8799 & sleep 1; curl -sI http://localhost:8799/app.js | grep -i content-type; curl -sI http://localhost:8799/ | head -1; kill %1
```
Expected: `Content-Type: text/javascript; charset=utf-8` per `app.js`; `HTTP/1.1 200 OK` per `/`.

---

### Task 2: Playwright devDep + config + gitignore

**Files:**
- Modify: `package.json` (devDependencies)
- Create: `playwright.config.js`
- Modify: `.gitignore`

- [ ] **Step 1: Installa @playwright/test e il browser**

Run:
```bash
npm install --save-dev --save-exact @playwright/test
npx playwright install chromium
```
Expected: `@playwright/test` a versione esatta in `devDependencies`; chromium scaricato (una-tantum).

- [ ] **Step 2: Scrivi la config**

`playwright.config.js`:
```js
import { defineConfig, devices } from "@playwright/test";

const PORT = 8766;

export default defineConfig({
  testDir: "./e2e",
  fullyParallel: true,
  reporter: "list",
  use: {
    baseURL: `http://localhost:${PORT}`,
    trace: "off",
  },
  projects: [{ name: "chromium", use: { ...devices["Desktop Chrome"] } }],
  webServer: {
    command: `node scripts/static-server.cjs ${PORT}`,
    url: `http://localhost:${PORT}`,
    reuseExistingServer: !process.env.CI,
    timeout: 30000,
  },
});
```

- [ ] **Step 3: Ignora gli artefatti E2E**

In `.gitignore`, aggiungi in fondo:
```
test-results/
playwright-report/
```

---

### Task 3: I test di boot

**Files:**
- Create: `e2e/boot.spec.js`

- [ ] **Step 1: Scrivi i 2 test**

`e2e/boot.spec.js`:
```js
import { test, expect } from "@playwright/test";

const CDN_RE = /esm\.sh|cdn\.jsdelivr|unpkg\.com|skypack/;

test("boot online: shell montata, niente CDN, niente errori", async ({ page }) => {
  const errors = [];
  const cdnHits = [];
  page.on("console", (m) => { if (m.type() === "error") errors.push(m.text()); });
  page.on("pageerror", (e) => errors.push("pageerror: " + e.message));
  page.on("request", (r) => { if (CDN_RE.test(r.url())) cdnHits.push(r.url()); });

  await page.goto("/");
  await expect(page.getByText("autenticazione richiesta")).toBeVisible();
  await expect(page).toHaveTitle("set.log");
  expect(cdnHits, "nessuna richiesta a CDN").toEqual([]);
  expect(errors, "nessun errore console/page").toEqual([]);
});

test("boot offline: l'app fa boot dalla cache del SW", async ({ page, context }) => {
  const errors = [];
  page.on("console", (m) => { if (m.type() === "error") errors.push(m.text()); });
  page.on("pageerror", (e) => errors.push("pageerror: " + e.message));

  await page.goto("/");
  await page.evaluate(() => navigator.serviceWorker.ready);
  await page.waitForTimeout(800); // margine: l'install ha già cachato gli ASSETS

  await context.setOffline(true);
  await page.reload();
  await expect(page.getByText("autenticazione richiesta")).toBeVisible();
  expect(errors, "nessun errore offline").toEqual([]);
  await context.setOffline(false);
});
```

- [ ] **Step 2: Run E2E — verde**

Run: `npx playwright test`
Expected: 2 passed. Se "boot online" fallisce per un errore console benigno inatteso,
ispezionarlo: se è un vero errore dell'app, è un bug da sistemare; se è rumore esterno
non riproducibile, restringere il filtro (ma NON silenziare errori reali dell'app).

---

### Task 4: Script npm + README + commit

**Files:**
- Modify: `package.json` (scripts)
- Modify: `README.md`

- [ ] **Step 1: Aggiungi lo script e2e**

In `package.json`, dentro `"scripts"`, aggiungi accanto a `"test"`:
```json
    "test": "node --test",
    "e2e": "playwright test"
```

- [ ] **Step 2: Documenta nel README**

In `README.md`, nella sezione "Sviluppo locale", dopo la riga `npm test`, aggiungi:
```
npx playwright install chromium   # una-tantum: scarica il browser per gli E2E
npm run e2e                        # smoke test E2E (boot online + offline)
```

- [ ] **Step 3: Verifica entrambe le suite**

Run: `npm test`
Expected: 446 passed, 0 fail.

Run: `npm run e2e`
Expected: 2 passed.

- [ ] **Step 4: Verifica niente artefatti sporchi**

Run: `git status -s`
Expected: solo file sorgente nuovi/modificati (`scripts/static-server.cjs`,
`playwright.config.js`, `e2e/boot.spec.js`, `package.json`, `package-lock.json`,
`.gitignore`, `README.md`). NESSUNA cartella `test-results/` o `playwright-report/`.

- [ ] **Step 5: Commit**

```bash
git add scripts/static-server.cjs playwright.config.js e2e/boot.spec.js package.json package-lock.json .gitignore README.md
git commit -m "test(e2e): smoke Playwright boot online+offline + static server + runner"
```

---

## Note di chiusura

- I binari del browser stanno nella cache di Playwright (fuori dal repo), non vengono spediti.
- A integrazione completata: flusso `finishing-a-development-branch` (ff-merge su main + push).
- Possibile incremento separato (NON in questo piano): micro-CI GitHub Actions che gira
  `npm test` + `npm run e2e` a ogni push; e in futuro scope C (E2E autenticato con Supabase
  mockato).
