# Demo Mode (soglia "Boot CLI" + ospite locale) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sostituire il muro di login con una soglia "Boot CLI" che offre una **demo solo-locale** (identità ospite, nessun account, dati finti curati), con uscita guardata che protegge chi si allena davvero (export file + "registrati e tieni i tuoi dati").

**Architecture:** La demo riusa l'infrastruttura esistente al 95%. Un nuovo `LocalStore` replica l'interfaccia `load()/save()` di `SupabaseStore` ma su `localStorage`, così diventa un drop-in nel `pusher`. L'identità ospite usa `ProfileStorage` con `uid = "demo-guest"` (isolamento totale dai dati veri). Un seed puro (`demo-seed.js`) genera una scheda Push/Pull/Gambe + 3 settimane di log; lo **Scan si accende da solo** perché deriva dai log. La soglia è una nuova schermata mostrata in `boot()` al posto di `#auth-screen` quando non c'è sessione né demo attiva.

**Tech Stack:** Vanilla JS ESM, `node --test` (unit), Playwright (`npm run e2e`), Supabase (solo lato account vero), localStorage (demo). Nessuna nuova dipendenza.

**Decisioni di prodotto (chiuse col grill 2026-06-15):**
- (a) Demo **persiste tra reload**; "esci dalla demo" è teardown volontario.
- (b) Link vetrina → **sempre soglia** (niente deep-link `?demo=1`).
- (D2) "Esci" è **guardato**: se ci sono dati dell'utente offre *esporta file* o *registrati portando i dati*; altrimenti esce e pulisce. Export + migrazione-al-signup sono gli **ultimi task**.
- (D3) Indicatore: **barra demo persistente** ("● demo · dati finti" + "registrati"); "esci dalla demo" vive in **Impostazioni** (al posto del blocco account/logout).
- (Scan) Deriva interamente dai set loggati (`body.js`) → il seed **non** contiene dati-misura.

---

## File Structure

**Nuovi file:**
- `local-store.js` — classe `LocalStore` (drop-in di `SupabaseStore` su `localStorage`). Puro/testabile.
- `demo.js` — flag sessione demo + lifecycle: `isDemoActive`, `enterDemo`, `exitDemo`, `DEMO_UID`. Puro/testabile.
- `demo-seed.js` — `seedDemoData(now)` (blob schema-6 curato) + `isDemoModified(blob, now)`. Puro/testabile.
- `demo-export.js` — `buildDemoExport(blob, now)` → `{ filename, json }`. Puro/testabile.
- `tests/local-store.test.js`, `tests/demo.test.js`, `tests/demo-seed.test.js`, `tests/demo-export.test.js`.

**File modificati:**
- `index.html` — nuova `<section id="threshold-screen">` (soglia Boot CLI) + barra demo `#demo-bar` + sezione demo in Impostazioni.
- `app.js` — `boot()` (gate a `:1859`): rami soglia/demo; estrazione `wireAppEventListeners()` da `:1916-1999`; nuova `bootDemo()`; cablaggio soglia e barra.
- `auth.js` — `bindAuthScreen`: opzione per partire su un tab specifico + hook post-signup per la migrazione demo→account.
- `style.css` (o il foglio attuale) — token soglia/barra (già combaciano col tema graphite; presi dal mockup `mockups/soglia-demo-rev1.html`).
- `sw.js` — bump versione cache + aggiungere i nuovi `.js` alla precache list.
- `tests/e2e/` — smoke del percorso demo.

**Riferimenti chiave (verificati a 5d35864):**
- `app.js:1847` `boot()`; `:1859` gate `if (!session)`; `:1916-1999` wiring UI condiviso; `:2044-2052` seed catalogo.
- `store.js:312` `SupabaseStore` (`load`/`save`), `:27` `emptyData()`, `:43` `setEntry`, `:87-132` normalizzatori.
- `profile-storage.js:7` `ProfileStorage(storage, uid)`, `.clear()` a `:35`.
- `sheets.js:56` `hydrate`, `:74` `dehydrate`, `:19` `toSheetsBlob` (blob schema 6).
- `sync.js:4` `createPusher`.
- `catalog.js:97` `seedCatalogIfAbsent(blob)`, voce `{id,name,muscle,note,secondary[]}`.
- `auth.js:115` `hideAuthScreen()`, `:120` `bindAuthScreen(client,{onLoggedIn,redirectTo})`.
- `splash-control.js:13` `splashBootReady()`.
- `plan.js:6` `PLAN` (shape esercizio/giornata); `editor.js:4` `genId`.
- Mock localStorage di test: `tests/profile-storage.test.js:5-15` (`memStorage()`).

---

## PHASE 1 — Moduli puri (TDD, nessun DOM)

> Questi 4 moduli non toccano la UI: si fanno interamente in TDD con `node --test`. Sono il fondamento e il passo "iniziare in TDD" dell'handoff.

### Task 1: `LocalStore` — store locale drop-in di `SupabaseStore`

**Files:**
- Create: `local-store.js`
- Test: `tests/local-store.test.js`

- [ ] **Step 1: Scrivi il test che fallisce**

```js
// tests/local-store.test.js
import { test } from "node:test";
import assert from "node:assert/strict";
import { LocalStore } from "../local-store.js";

function memStorage() {
  const m = new Map();
  return {
    getItem: (k) => (m.has(k) ? m.get(k) : null),
    setItem: (k, v) => m.set(k, String(v)),
    removeItem: (k) => m.delete(k),
    key: (i) => Array.from(m.keys())[i] ?? null,
    get length() { return m.size; },
  };
}

test("load: store vuoto → emptyData() + version 0", async () => {
  const s = new LocalStore(memStorage());
  const { data, version } = await s.load();
  assert.equal(version, 0);
  assert.deepEqual(data, { updatedAt: null, weeks: {}, plan: [], schema: 5 });
});

test("save poi load: ritorna il blob e incrementa la version", async () => {
  const st = memStorage();
  const s = new LocalStore(st);
  const v1 = await s.save({ schema: 6, sheets: [] }, 0);
  assert.equal(v1, 1);
  const v2 = await s.save({ schema: 6, sheets: [{ id: "x" }] }, v1);
  assert.equal(v2, 2);
  const { data, version } = await s.load();
  assert.equal(version, 2);
  assert.deepEqual(data, { schema: 6, sheets: [{ id: "x" }] });
});

test("costruttore senza storage → errore esplicito", () => {
  assert.throws(() => new LocalStore(null), /storage/);
});
```

- [ ] **Step 2: Esegui il test e verifica che fallisca**

Run: `node --test tests/local-store.test.js`
Expected: FAIL — `Cannot find module '../local-store.js'`.

- [ ] **Step 3: Implementa il minimo**

```js
// local-store.js
// Drop-in di SupabaseStore (store.js:312) ma su localStorage: stessa interfaccia
// load()/save() così il pusher (sync.js) lo usa identico. Single-device: niente
// optimistic locking reale, la version è solo un contatore monotòno.
import { emptyData } from "./store.js";

const DEFAULT_KEY = "gymsched_demo_store";

export class LocalStore {
  constructor(storage, key = DEFAULT_KEY) {
    if (!storage) throw new Error("LocalStore richiede uno storage backend");
    this.storage = storage;
    this.key = key;
  }

  // Ritorna { data, version }. Store assente o corrotto → emptyData()+0 (verrà
  // riseminato dal seed-if-absent in bootDemo). Niente fallback silenzioso che
  // "finge": reseed è il comportamento corretto, non un errore nascosto.
  async load() {
    const raw = this.storage.getItem(this.key);
    if (raw == null) return { data: emptyData(), version: 0 };
    const parsed = JSON.parse(raw); // se corrotto, lancia: lo gestisce il chiamante
    return { data: parsed.data, version: parsed.version ?? 0 };
  }

  // Incrementa la version e salva { data, version }. Ritorna newVersion.
  async save(blob, expectedVersion) {
    const next = (Number(expectedVersion) || 0) + 1;
    this.storage.setItem(this.key, JSON.stringify({ data: blob, version: next }));
    return next;
  }
}
```

- [ ] **Step 4: Esegui il test e verifica che passi**

Run: `node --test tests/local-store.test.js`
Expected: PASS (3 test).

- [ ] **Step 5: Commit**

```bash
git add local-store.js tests/local-store.test.js
git commit -m "feat(demo): LocalStore, drop-in di SupabaseStore su localStorage"
```

---

### Task 2: `demo.js` — flag sessione demo + lifecycle

**Files:**
- Create: `demo.js`
- Test: `tests/demo.test.js`

- [ ] **Step 1: Scrivi il test che fallisce**

```js
// tests/demo.test.js
import { test } from "node:test";
import assert from "node:assert/strict";
import { isDemoActive, enterDemo, exitDemo, DEMO_UID } from "../demo.js";

function memStorage() {
  const m = new Map();
  return {
    getItem: (k) => (m.has(k) ? m.get(k) : null),
    setItem: (k, v) => m.set(k, String(v)),
    removeItem: (k) => m.delete(k),
    key: (i) => Array.from(m.keys())[i] ?? null,
    get length() { return m.size; },
  };
}

test("demo non attiva di default", () => {
  assert.equal(isDemoActive(memStorage()), false);
});

test("enterDemo attiva il flag, persiste a un reload simulato", () => {
  const st = memStorage();
  enterDemo(st);
  assert.equal(isDemoActive(st), true); // sopravvive: il flag resta in storage
});

test("exitDemo pulisce flag, store demo e namespace ospite", () => {
  const st = memStorage();
  enterDemo(st);
  st.setItem("gymsched_demo_store", "{}");
  st.setItem(`gymsched_user_${DEMO_UID}_data`, "{}");
  st.setItem("gymsched_user_REALUID_data", "{}"); // dati veri: NON toccare
  exitDemo(st);
  assert.equal(isDemoActive(st), false);
  assert.equal(st.getItem("gymsched_demo_store"), null);
  assert.equal(st.getItem(`gymsched_user_${DEMO_UID}_data`), null);
  assert.equal(st.getItem("gymsched_user_REALUID_data"), "{}"); // intatto
});
```

- [ ] **Step 2: Esegui e verifica fallimento**

Run: `node --test tests/demo.test.js`
Expected: FAIL — modulo inesistente.

- [ ] **Step 3: Implementa**

```js
// demo.js
// Lifecycle della modalità demo (ospite locale). Il flag vive in localStorage
// "nudo" (non namespacizzato) perché boot() lo legge PRIMA di sapere chi è
// l'utente. I dati demo vivono sotto ProfileStorage(uid="demo-guest"), isolati.
import { ProfileStorage } from "./profile-storage.js";

export const DEMO_UID = "demo-guest";
const DEMO_FLAG = "gymsched_demo_active";
const DEMO_STORE_KEY = "gymsched_demo_store"; // stessa chiave di LocalStore di default

export function isDemoActive(storage) {
  return storage.getItem(DEMO_FLAG) === "1";
}

export function enterDemo(storage) {
  storage.setItem(DEMO_FLAG, "1");
}

// Teardown completo: flag + store demo + tutte le chiavi del profilo ospite.
// Non tocca MAI le chiavi di un utente reale (namespace diverso).
export function exitDemo(storage) {
  storage.removeItem(DEMO_FLAG);
  storage.removeItem(DEMO_STORE_KEY);
  new ProfileStorage(storage, DEMO_UID).clear();
}
```

- [ ] **Step 4: Esegui e verifica PASS**

Run: `node --test tests/demo.test.js` → PASS (3 test).

- [ ] **Step 5: Commit**

```bash
git add demo.js tests/demo.test.js
git commit -m "feat(demo): flag sessione + lifecycle enter/exit ospite"
```

---

### Task 3: `demo-seed.js` — seed curato + rilevamento "modificato"

**Files:**
- Create: `demo-seed.js`
- Test: `tests/demo-seed.test.js`

> Il seed produce un **blob schema-6** (compatibile con `hydrate`): 1 scheda Push/Pull/Gambe (`A`/`B`/`C`), un superset su A, 3 settimane ISO consecutive di log con carichi progressivi, catalogo standard (`seedCatalogIfAbsent`). I nomi esercizio coincidono con quelli del catalogo seed così lo Scan mappa anche i secondari. `now` è iniettato per determinismo nei test.

- [ ] **Step 1: Scrivi il test che fallisce**

```js
// tests/demo-seed.test.js
import { test } from "node:test";
import assert from "node:assert/strict";
import { seedDemoData, isDemoModified } from "../demo-seed.js";
import { hydrate, activeSheet } from "../sheets.js";
import { isoWeekKey } from "../store.js";

const NOW = new Date("2026-06-15T10:00:00Z"); // lunedì fisso → keys deterministiche

test("seed: blob schema 6 con 1 scheda e catalogo", () => {
  const b = seedDemoData(NOW);
  assert.equal(b.schema, 6);
  assert.equal(b.sheets.length, 1);
  assert.ok(Array.isArray(b.catalog) && b.catalog.length > 0);
});

test("seed: scheda Push/Pull/Gambe con giorni A/B/C", () => {
  const sh = activeSheet(seedDemoData(NOW));
  assert.deepEqual(sh.plan.map((d) => d.day), ["A", "B", "C"]);
  assert.ok(sh.plan.every((d) => d.exercises.length >= 4));
});

test("seed: un superset sul giorno A", () => {
  const sh = activeSheet(seedDemoData(NOW));
  const a = sh.plan.find((d) => d.day === "A");
  assert.equal(a.exercises.filter((e) => e.superset).length, 1);
});

test("seed: 3 settimane loggate con date", () => {
  const sh = activeSheet(seedDemoData(NOW));
  const wks = Object.keys(sh.weeks);
  assert.equal(wks.length, 3);
  assert.ok(wks.includes(isoWeekKey(NOW))); // la settimana corrente è inclusa
  for (const wk of wks) assert.ok(Object.keys(sh.weeks[wk].dates).length >= 1);
});

test("seed: hydrate non lancia e proietta plan/weeks", () => {
  const mem = hydrate(seedDemoData(NOW));
  assert.ok(mem.plan.length === 3);
  assert.ok(Object.keys(mem.weeks).length === 3);
});

test("isDemoModified: seed fresco = non modificato; con edit = modificato", () => {
  const fresh = seedDemoData(NOW);
  assert.equal(isDemoModified(fresh, NOW), false);
  const edited = structuredClone(fresh);
  const wk = Object.keys(edited.sheets[0].weeks)[0];
  edited.sheets[0].weeks[wk].entries.A = { extraId: { sets: [{ reps: "5", kg: "99", done: true }], note: "" } };
  assert.equal(isDemoModified(edited, NOW), true);
});
```

- [ ] **Step 2: Esegui e verifica fallimento**

Run: `node --test tests/demo-seed.test.js` → FAIL (modulo inesistente).

- [ ] **Step 3: Implementa**

```js
// demo-seed.js
// Seed DEMO: blob schema-6 curato e DETERMINISTICO (a parità di `now`).
// Dati finti — mai i dati veri dell'utente. now iniettato per i test.
import { isoWeekKey } from "./store.js";
import { seedCatalogIfAbsent } from "./catalog.js";

const SHEET_ID = "demo-sheet";

// Giorni della scheda. id esercizio HARDCODATI e stabili: servono per chiavare i
// log (app.js exIdOf usa e.id) e per i grafici (openChartDialog(ex.id)).
function demoPlan() {
  return [
    { day: "A", title: "Spinta · Petto/Spalle/Tricipiti", exercises: [
      { id: "da1", name: "Panca piana bilanciere", setsReps: "4 × 6", recText: "2 min", restSeconds: 120, superset: false, muscle: "Petto", bar: 20 },
      { id: "da2", name: "Spinte manubri inclinata", setsReps: "3 × 8-10", recText: "90 sec", restSeconds: 90, superset: false, muscle: "Petto" },
      { id: "da3", name: "Croci ai cavi in piedi", setsReps: "3 × 12", recText: "75 sec", restSeconds: 75, superset: false, muscle: "Petto" },
      { id: "da4", name: "Alzate laterali + Pushdown tricipiti", setsReps: "3 × 15 / 3 × 12", recText: "60 sec", restSeconds: 60, superset: true, muscle: "Spalle", muscleB: "Tricipiti" },
    ] },
    { day: "B", title: "Tirata · Schiena/Bicipiti", exercises: [
      { id: "db1", name: "Stacco da terra", setsReps: "4 × 5", recText: "2.5 min", restSeconds: 150, superset: false, muscle: "Schiena", bar: 20 },
      { id: "db2", name: "Trazioni alla sbarra", setsReps: "4 × 8", recText: "2 min", restSeconds: 120, superset: false, muscle: "Schiena" },
      { id: "db3", name: "Rematore con bilanciere", setsReps: "3 × 10", recText: "90 sec", restSeconds: 90, superset: false, muscle: "Schiena", bar: 20 },
      { id: "db4", name: "Curl manubri", setsReps: "3 × 12", recText: "60 sec", restSeconds: 60, superset: false, muscle: "Bicipiti" },
    ] },
    { day: "C", title: "Gambe · Quadricipiti/Femorali", exercises: [
      { id: "dc1", name: "Squat con bilanciere", setsReps: "4 × 6", recText: "2.5 min", restSeconds: 150, superset: false, muscle: "Quadricipiti", bar: 20 },
      { id: "dc2", name: "Pressa", setsReps: "3 × 10", recText: "2 min", restSeconds: 120, superset: false, muscle: "Quadricipiti" },
      { id: "dc3", name: "Leg curl", setsReps: "3 × 12", recText: "75 sec", restSeconds: 75, superset: false, muscle: "Femorali" },
      { id: "dc4", name: "Calf in piedi", setsReps: "4 × 15", recText: "45 sec", restSeconds: 45, superset: false, muscle: "Polpacci" },
    ] },
  ];
}

// 3 chiavi settimana ISO consecutive che terminano nella settimana di `now`.
function lastThreeWeekKeys(now) {
  const keys = [];
  for (let i = 2; i >= 0; i--) {
    const d = new Date(now);
    d.setDate(d.getDate() - i * 7);
    keys.push(isoWeekKey(d));
  }
  return keys;
}

// Una serie loggata "fatta".
const S = (reps, kg) => ({ reps: String(reps), kg: String(kg), done: true, feel: "", warmup: false, failed: false, failNote: "", comments: [] });

// Entry per un esercizio normale (n serie a stesso reps/kg) o superset (a/b).
function entryFor(ex, kg) {
  if (ex.superset) {
    return {
      a: { sets: [S(15, kg.a), S(15, kg.a), S(15, kg.a)], note: "" },
      b: { sets: [S(12, kg.b), S(12, kg.b), S(12, kg.b)], note: "" },
      c: { sets: [], note: "" },
      note: "",
    };
  }
  return { sets: [S(ex._reps, kg), S(ex._reps, kg), S(ex._reps, kg)], note: "" };
}

// Carico per esercizio e per indice-settimana (0,1,2): progressione +incremento.
function kgFor(ex, w) {
  if (ex.superset) return { a: 8 + w * 2, b: 14 + w * 2.5 };       // alzate / pushdown
  const base = { da1: 60, da2: 24, da3: 12, db1: 90, db2: 0, db3: 50, db4: 12, dc1: 80, dc2: 120, dc3: 35, dc4: 60 }[ex.id] ?? 20;
  return Math.round((base + w * 2.5) * 10) / 10;
}

export function seedDemoData(now = new Date()) {
  const plan = demoPlan();
  const weekKeys = lastThreeWeekKeys(now);
  const weeks = {};
  weekKeys.forEach((wk, w) => {
    // Una data per giorno dentro la settimana (lun/mer/ven), derivata da `now`.
    const monday = new Date(now);
    monday.setDate(monday.getDate() - (w === 2 ? 0 : (2 - w) * 7) - ((monday.getDay() + 6) % 7));
    const dateFor = (offset) => {
      const d = new Date(monday); d.setDate(d.getDate() + offset);
      return d.toISOString().slice(0, 10);
    };
    const entries = { A: {}, B: {}, C: {} };
    for (const d of plan) {
      for (const ex of d.exercises) {
        const reps = parseInt(String(ex.setsReps).split("×")[1], 10) || 8;
        entries[d.day][ex.id] = entryFor({ ...ex, _reps: reps }, kgFor(ex, w));
      }
    }
    weeks[wk] = {
      label: wk,
      entries,
      dates: { A: dateFor(0), B: dateFor(2), C: dateFor(4) },
    };
  });

  const blob = {
    schema: 6,
    updatedAt: now.toISOString(),
    activeSheetId: SHEET_ID,
    sheets: [{ id: SHEET_ID, name: "Scheda demo", plan, weeks }],
  };
  return seedCatalogIfAbsent(blob); // aggiunge blob.catalog standard
}

// "Modificato" = il blob differisce dal seed fresco (a parità di `now`).
// Confronto strutturale: se l'utente ha loggato/editato qualcosa, cambia.
export function isDemoModified(blob, now = new Date()) {
  return JSON.stringify(blob) !== JSON.stringify(seedDemoData(now));
}
```

> Nota di implementazione: se in Step 4 un test sulle *date* fallisse per via dell'aritmetica `monday`, semplificare `dateFor` usando direttamente `weekKeys` con date ancorate (es. tutte basate su `now` − offset fisso). Il contratto testato è: 3 settimane, ognuna con `dates` per A/B/C e la settimana corrente inclusa. Adatta il calcolo finché i 6 test passano — non cambiare il contratto.

- [ ] **Step 4: Esegui e verifica PASS**

Run: `node --test tests/demo-seed.test.js` → PASS (6 test). Se il test "3 settimane loggate con date" fallisce, correggi `dateFor`/`lastThreeWeekKeys` come da nota sopra, poi ri-esegui.

- [ ] **Step 5: Commit**

```bash
git add demo-seed.js tests/demo-seed.test.js
git commit -m "feat(demo): seed curato Push/Pull/Gambe + 3 settimane log"
```

---

### Task 4: `demo-export.js` — export del blob demo

**Files:**
- Create: `demo-export.js`
- Test: `tests/demo-export.test.js`

- [ ] **Step 1: Scrivi il test che fallisce**

```js
// tests/demo-export.test.js
import { test } from "node:test";
import assert from "node:assert/strict";
import { buildDemoExport } from "../demo-export.js";

const NOW = new Date("2026-06-15T10:00:00Z");

test("buildDemoExport: filename datato + JSON valido del blob", () => {
  const blob = { schema: 6, sheets: [{ id: "x", name: "Scheda demo" }] };
  const { filename, json } = buildDemoExport(blob, NOW);
  assert.match(filename, /^set-log-demo-2026-06-15\.json$/);
  assert.deepEqual(JSON.parse(json), blob);
});
```

- [ ] **Step 2: Esegui e verifica fallimento** — `node --test tests/demo-export.test.js` → FAIL.

- [ ] **Step 3: Implementa**

```js
// demo-export.js
// Costruisce il payload di export del blob demo (puro). Il download vero (Blob +
// <a download>) sta nel cablaggio UI: qui solo dati, così è testabile in Node.
export function buildDemoExport(blob, now = new Date()) {
  const day = now.toISOString().slice(0, 10);
  return {
    filename: `set-log-demo-${day}.json`,
    json: JSON.stringify(blob, null, 2),
  };
}
```

- [ ] **Step 4: Esegui e verifica PASS** — `node --test tests/demo-export.test.js` → PASS.

- [ ] **Step 5: Commit**

```bash
git add demo-export.js tests/demo-export.test.js
git commit -m "feat(demo): buildDemoExport (payload JSON puro)"
```

- [ ] **Step 6: Checkpoint Fase 1** — `npm test` (atteso: 447 precedenti + nuovi, 0 fail).

---

## PHASE 2 — Soglia + boot demo + UI (DOM, e2e)

> Qui si tocca `index.html`/`app.js`/CSS. I pezzi non-unit-testabili si verificano con `npm run e2e` (Playwright) e con verifica manuale nel browser (mai dichiarare "fatto" senza aver aperto l'app).

### Task 5: Estrarre il wiring UI condiviso da `boot()`

**Files:**
- Modify: `app.js:1916-1999` → estrai in `function wireAppEventListeners()`.

**Perché:** il blocco di `addEventListener` (weekSelect, dayTabs, dialoghi, drawer, calendario, ecc.) non dipende dalla sessione. Serve identico sia al boot autenticato sia a `bootDemo()`. Estrarlo evita di duplicarlo e tiene un solo punto di verità. La rete di sicurezza è `npm run e2e` (il boot autenticato deve restare identico).

- [ ] **Step 1:** Crea `function wireAppEventListeners() { ... }` contenente **esattamente** le righe `app.js:1916-1999` (da `wireTimerControls();` fino alla chiusura del listener `popstate`). Non cambiare il contenuto.
- [ ] **Step 2:** In `boot()` (path autenticato) sostituisci quel blocco con una sola chiamata `wireAppEventListeners();`. Le righe specifiche-sessione (`accountEmail`, `btnLogout`, `onAuthStateChange`, `:1896-1907`) **restano** nel path autenticato, fuori dalla funzione estratta.
- [ ] **Step 3:** Run `npm run e2e` — atteso: gli smoke boot online+offline passano (nessuna regressione).
- [ ] **Step 4:** Commit `refactor(app): estrai wireAppEventListeners (condiviso boot/demo)`.

### Task 6: Soglia "Boot CLI" in `index.html` + CSS

**Files:**
- Modify: `index.html` (nuova `<section id="threshold-screen" hidden>` prima/accanto a `#auth-screen`).
- Modify: foglio di stile (token già nel mockup `mockups/soglia-demo-rev1.html:11-156`, variante `#v1`).

- [ ] **Step 1:** Porta il markup della **variante ①** del mockup (`#v1`, righe `266-285`) in una `<section id="threshold-screen" hidden>` reale: badge SVG + brand, blocco `.term` (system ready / sessione: ospite / seleziona modalità), bottone `data-act="demo"` ("$ prova la demo"), link `data-act="auth" data-tab="login|signup"`, footer "// la demo gira in locale".
- [ ] **Step 2:** Porta i CSS relativi (`.term`, `.cta`, `.tlink`, `.cur`, `#threshold-screen` layout) adattando i selettori al foglio dell'app; i token (`--acc`, `--surf`, `--mono`…) esistono già nel tema graphite. Rispetta `prefers-reduced-motion` (cursore `.cur` senza blink) come nel mockup `:234-238`.
- [ ] **Step 3 (verifica):** apri l'app con sessione assente (vedi Task 8 per il gate) e controlla a occhio + screenshot Playwright che la soglia compaia al posto del login. Mobile-first, tap target ≥44px.
- [ ] **Step 4:** Commit `feat(demo): soglia Boot CLI in index.html + stili`.

### Task 7: Cablaggio soglia (mostra/azioni) in `app.js`

**Files:**
- Modify: `app.js` — nuova `showThreshold()` + listener sui bottoni soglia.

- [ ] **Step 1:** `showThreshold()`: `#threshold-screen.hidden=false`, `#auth-screen.hidden=true`, `#app.hidden=true`, poi `splashBootReady()`.
- [ ] **Step 2:** Listener sui `[data-act]` della soglia:
  - `demo` → `enterDemo(localStorage); location.reload();` (reload = boot rientra nel ramo demo; semplice e robusto, stesso pattern di `onLoggedIn: location.reload()`).
  - `auth` → nascondi soglia, mostra `#auth-screen`, apri sul tab `data-tab` (login/signup) — richiede Task 9 (param tab in `bindAuthScreen`).
  - dal pannello auth, "← torna alla soglia" → `showThreshold()`.
- [ ] **Step 3 (verifica manuale):** in browser, click "prova la demo" → reload → app demo; click "accedi"/"registrati" → form sul tab giusto; back → soglia.
- [ ] **Step 4:** Commit `feat(demo): cablaggio azioni soglia (demo/accedi/registrati)`.

### Task 8: Gate in `boot()` + `bootDemo()`

**Files:**
- Modify: `app.js:1859` (gate `if (!session)`), nuova `async function bootDemo()`.

- [ ] **Step 1:** Modifica il gate a `:1859`:

```js
if (!session) {
  if (isDemoActive(localStorage)) { return bootDemo(); }
  showThreshold();         // soglia al posto del muro di login
  return;
}
// Path autenticato: per sicurezza, se per qualche motivo il flag demo è rimasto
// settato ma c'è una sessione vera, puliscilo (la sessione vera ha precedenza).
localStorage.removeItem("gymsched_demo_active");
```

- [ ] **Step 2:** `bootDemo()` — sottoinsieme demo del path autenticato (niente Supabase/sessione):

```js
async function bootDemo() {
  hideAuthScreen();                       // mostra #app
  document.getElementById("threshold-screen").hidden = true;
  profileStorage = new ProfileStorage(localStorage, DEMO_UID);
  applyTheme(document.documentElement, localStorage);
  applyFx(document.body, localStorage);
  store = new LocalStore(localStorage);

  // Seed-if-absent: prima volta (o dopo exit) → semina e salva.
  let remote;
  try { remote = await store.load(); }
  catch { remote = { data: emptyData(), version: 0 }; } // store corrotto → reseed
  if (remote.version === 0 || planIsEmpty(hydrate(remote.data))) {
    const seeded = seedDemoData();
    dataVersion = await store.save(seeded, 0);
    data = hydrate(seeded);
    profileStorage.set("data", data);
    profileStorage.set("version", dataVersion);
  } else {
    data = hydrate(remote.data);
    dataVersion = remote.version;
    profileStorage.set("data", data);
  }

  pusher = createPusher({
    getData: () => dehydrate(data),
    getVersion: () => dataVersion,
    setVersion: (v) => { dataVersion = v; profileStorage.set("version", v); },
    setDirty: (d) => profileStorage.set("dirty", d),
    store,
    onStatus: (s) => setStatus({ pending: "salvo ⧗", ok: "ok ✓", error: "errore ⧗" }[s], s),
    // niente onConflict/onAuthError: LocalStore non li lancia mai.
  });

  wireAppEventListeners();   // wiring condiviso (Task 5)
  wireSettings();
  wireDemoBar();             // Task 10
  render();
  setStatus("ok ✓", "ok");
  splashBootReady();
}
```

- [ ] **Step 3 (verifica):** entra in demo → l'app si popola col seed (3 giornate, calendario con 3 settimane, Scan acceso). Reload → resta in demo coi dati. Log di una serie → al reload persiste.
- [ ] **Step 4 (e2e):** estendi la suite: boot → soglia visibile → click demo → `#app` visibile con scheda demo → reload → ancora demo.
- [ ] **Step 5:** Commit `feat(demo): gate boot + bootDemo (LocalStore + seed-if-absent)`.

### Task 9: `bindAuthScreen` — tab iniziale

**Files:**
- Modify: `auth.js:120` (`bindAuthScreen`).

- [ ] **Step 1:** Aggiungi param opzionale `initialTab` (`"login"|"signup"`) e un metodo/handler per impostare il tab attivo da fuori (es. esporta `setAuthTab(tab)` o accetta `bindAuthScreen(client, { onLoggedIn, redirectTo, initialTab })`). Usa il pattern tab già presente (`.auth-tab.is-active`).
- [ ] **Step 2 (test):** se `auth.js` ha già test, aggiungi un caso "initialTab=signup → tab signup attivo"; altrimenti verifica manuale (auth.js dipende dal DOM).
- [ ] **Step 3:** Commit `feat(auth): tab iniziale per ingresso da soglia`.

---

## PHASE 3 — Barra demo, uscita guardata, export, migrazione

### Task 10: Barra demo persistente

> **Decisione visiva APPROVATA 2026-06-16** (mockup `mockups/demo-bar-rev1.html`, variante **A "mode line"**): strip sottile full-bleed in cima alla colonna (sopra `.wrap`, dentro `#app`), sfondo `--acc-soft` + bordo `--line-acc`. Sinistra: pallino `--acc` + `demo · dati finti` (`--ac2`/`--dim`). Destra: `registrati ›` come link ghost `--acc` (niente bottone pieno: scelta meno "venditore" della variante C, scartata). Pallino pieno e leggibile da fermo; pulsa solo dietro `@media (prefers-reduced-motion: no-preference)` (utente ha Riduci movimento ON). Tap target ≥44px. Funziona in Carta + Graphite.

**Files:**
- Modify: `index.html` (`#demo-bar` sottile, hidden di default), `app.js` (`wireDemoBar()`), CSS.

- [ ] **Step 1:** `#demo-bar`: "● demo · dati finti" + bottone "registrati". Mostrata solo in `bootDemo()` (`wireDemoBar()` la rende visibile). Sfondo coerente col tag `.demobody .tag` del mockup (`:222`).
- [ ] **Step 2:** "registrati" → nascondi `#app`, mostra `#auth-screen` su tab signup (riusa Task 9), con flag "veniamo dalla demo" per la migrazione (Task 13).
- [ ] **Step 3 (verifica):** barra visibile solo in demo, mai nell'account vero; non ruba spazio ai contenuti (controlla `--bottom-pad`/safe-area).
- [ ] **Step 4:** Commit `feat(demo): barra demo persistente con CTA registrati`.

### Task 11: Sezione demo in Impostazioni (al posto di account/logout)

**Files:**
- Modify: `app.js` (`wireSettings()` / sezione account), `index.html` (blocco Impostazioni).

- [ ] **Step 1:** In demo, nascondi il blocco account reale (`accountEmail`, `btnLogout`, import/recover legacy `:1905-1907`) e mostra un blocco demo: testo "Modalità demo · dati finti" + bottone "Esci dalla demo" + "Esporta i miei dati".
- [ ] **Step 2:** Distinzione demo/reale: usa `isDemoActive(localStorage)` per scegliere quale blocco montare in `wireSettings()`.
- [ ] **Step 3 (verifica):** in demo, Impostazioni mostra il blocco demo; nell'account reale mostra account+logout invariati.
- [ ] **Step 4:** Commit `feat(demo): sezione demo in Impostazioni`.

### Task 12: Uscita guardata + export (download)

**Files:**
- Modify: `app.js` — handler "Esci dalla demo" e "Esporta", usando `isDemoModified` (Task 3) e `buildDemoExport` (Task 4).

- [ ] **Step 1:** "Esci dalla demo":
  - se `isDemoModified(dehydrate(data))` è `false` → `exitDemo(localStorage); location.reload();` (torna alla soglia, reseed al prossimo ingresso).
  - se `true` → dialog di conferma con 3 vie: **Esporta** (download), **Registrati per tenerli** (→ auth signup, Task 13), **Esci e cancella** (`exitDemo` + reload).
- [ ] **Step 2:** "Esporta": `const {filename, json} = buildDemoExport(dehydrate(data)); ` poi crea `Blob([json],{type:"application/json"})`, URL oggetto, `<a download=filename>` click programmatico, revoke URL. (Questa parte è DOM → verifica manuale: il file scaricato è un JSON valido del blob.)
- [ ] **Step 3 (verifica):** demo non modificata → esci diretto; demo modificata → dialog; export scarica `set-log-demo-YYYY-MM-DD.json` apribile.
- [ ] **Step 4:** Commit `feat(demo): uscita guardata + export JSON`.

### Task 13: Migrazione demo → nuovo account al signup

**Files:**
- Modify: `auth.js` / `app.js` — al login/signup riuscito *provenendo dalla demo*, offri di portare i dati nel nuovo account.

> Questo è il funnel di conversione e il task più delicato (tocca il save Supabase reale). Farlo per ultimo, con la demo già funzionante.

- [ ] **Step 1:** Quando si apre auth *dalla demo* (soglia "registrati", barra "registrati", nudge), marca un flag effimero in memoria/sessionStorage `gymsched_demo_migrate=1`.
- [ ] **Step 2:** In `boot()` path autenticato, **dopo** aver costruito `store` (SupabaseStore) e prima del primo render, se `gymsched_demo_migrate` è settato E esiste un blob demo (`new LocalStore(localStorage).load()` con version>0 e dati non vuoti): chiedi all'utente "Portare i dati della demo in questo account?". Se sì → `await store.save(demoBlob, remote.version)` (rispettando la version del nuovo account), poi `exitDemo(localStorage)` e pulisci il flag. Se no → solo `exitDemo` + pulisci flag.
- [ ] **Step 3 (verifica manuale, ambiente di TEST):** crea un account di test, entra dalla demo, registrati → conferma migrazione → l'account contiene la scheda demo; rifiuta → account vuoto. **Mai** usare l'account/DB reale per questa prova.
- [ ] **Step 4:** Commit `feat(demo): migrazione opzionale dei dati demo al signup`.

### Task 14: Service worker + e2e finale

**Files:**
- Modify: `sw.js` (bump versione cache + precache dei nuovi `.js`), `tests/e2e/`.

- [ ] **Step 1:** Aggiungi `local-store.js`, `demo.js`, `demo-seed.js`, `demo-export.js` alla lista di precache del SW e bump della versione (es. `v97`→`v98`). Verifica offline: demo funziona senza rete (è tutta locale).
- [ ] **Step 2 (e2e):** percorso completo: soglia → demo → log una serie → reload (persiste) → Impostazioni → esci (modificata → dialog → esci e cancella) → soglia → demo di nuovo (seed fresco).
- [ ] **Step 3:** `npm test` + `npm run e2e` verdi.
- [ ] **Step 4:** Commit `feat(demo): SW precache moduli demo + e2e percorso completo`.

---

## Self-Review (eseguita)

- **Copertura spec:** soglia (T6/7), demo locale+persistenza (T1/2/8), seed che accende Scan (T3, Scan derivato confermato), barra+esci in Impostazioni (T10/11, = decisione D3), uscita guardata+export+migrazione (T12/13, = D2), link vetrina→soglia (gate T8, nessun parsing `?demo=1` = decisione b). ✔ tutte mappate.
- **Placeholder:** moduli puri con codice completo; i task DOM hanno spec concrete + anchor reali invece di codice keystroke-by-keystroke (scelta deliberata: il codice finale va scritto contro il DOM vivo, con e2e/verifica manuale come gate — coerente con la richiesta "piano breve" dell'utente).
- **Coerenza tipi:** `LocalStore.load/save` ≡ firma `SupabaseStore` (sync.js le usa identiche); blob seed schema-6 passa per `hydrate`/`dehydrate`; entry log = forma `normalizeSet`/`normalizeSupersetEntry`; id esercizio presenti per `exIdOf`/grafici. ✔
- **Rischi noti:** (1) date del seed — aritmetica settimana da validare coi test (nota in T3). (2) T13 tocca il save reale → ambiente di test, mai produzione. (3) estrazione T5 — coperta da e2e.

## Execution Handoff

Vedi messaggio in chat per la scelta del modo di esecuzione.
